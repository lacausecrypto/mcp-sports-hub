node_modules/
dist/
*.tsbuildinfo
.DS_Store
.env
.env.*
.mcpregistry_*
.npmrc

# Local-only Remotion presentation video project (not part of the package)
video/

# MCPB bundle build artifact (built from manifest.json via `npm run bundle`)
*.mcpb
